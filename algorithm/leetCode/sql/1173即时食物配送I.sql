select ifnull(round(count(delivery_id) /(select count(*)from Delivery)*100,2),0) as immediate_percentage from Delivery where order_date = customer_pref_delivery_date;
select round(sum(order_date=customer_pref_delivery_date)  /count(*)*100,2) as immediate_percentage from Delivery;