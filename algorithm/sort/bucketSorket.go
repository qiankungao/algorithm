package sort

/*

	桶排序：

*/

func BucketSort(array []float32) {
	//Step:1 	找出原数组的最大元素max,最小元素min

	//Step:2 创建n个通,每个桶的跨度为(max-min)/n

	//Step:3 把原数组的元素插入到对应的桶中

	//Step:4 给每个桶内的元素排序

	//Step:5 按照顺序，输出每个桶
}
